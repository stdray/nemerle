// A
protected virtual EqualsImpl(other : A.[]) : bool
{
  _  = other;
  match (other)
    {
    | null => 
      false
    | _  => 
      this.Field == other.Field && true
  }
}

// A
public Equals(other : A.[]) : bool
{
  EqualsImpl(other)
}

// A
public override Equals(other : System.Object) : boolmatch (other)
{
  | (x is A.[]) => 
    EqualsImpl(x)
  | _  => 
    false
}

// A
public override GetHashCode() : int
{
  unchecked 
  {
    mutable (hash : int);
    
    {
      
      {
        hash += Field;
        hash += hash << 10;
        hash ^= hash >> 6
      }
    };
    ();
    ();
    hash
  }
}

